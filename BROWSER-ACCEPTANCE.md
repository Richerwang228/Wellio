# 浏览器验收记录

日期：2026-09-12。验收地址：http://127.0.0.1:3120。使用 Codex 浏览器，功能操作仅为界面点击、输入提示词、刷新及观察可见结果；未用单元测试、接口调用或脚本替代功能验收。

本目录启用 live 生产构建，FastAPI、PostgreSQL、CopilotKit Runtime 和附件数据独立运行。模型调用 OpenRouter，搜索调用 Exa；训练、饮食及手表初始数据是标明来源的演示样例。

## 已完成的界面流程

| 操作 | 浏览器可见结果 |
| --- | --- |
| 我的 → 简体中文 | 页面与真实回答使用中文 |
| 输入“你好，我马上要演示这个应用，请简单介绍你可以怎么帮我。”，再问“我今天计划咋样？” | 真实回复读取当天训练、睡眠、饮食记录；后续对话正常 |
| 单独输入“你好”“谢谢”“hello” | 修复纯问候输出格式分支后，均真实完成回复；刷新仍保留已完成内容 |
| 输入“把今天已经记录的午餐份量改成一半。” | 修复后即时出现保存回执及修改、撤销按钮 |
| 追问“现在午餐和今天总共各是多少千卡？” | 午餐 525 kcal，当天 1125 kcal，与半份修改一致 |
| 点击份量修改回执的“撤销” | 显示“这次修改已撤销” |
| 输入“今天只有十五分钟，帮我生成一个精简训练方案。” | 显示真实未应用候选：35 → 15 分钟，保留划船和高位下拉 |
| 点击“应用调整” → “返回训练” → 刷新 Workout | 15 分钟、2 个动作及已验证负重持续显示 |
| 开始训练 → 完成两个动作 → 输入15分钟 → 保存并结束 → 返回 Today | Workout 和 Today 均显示训练已保存、2/2 完成 |
| 请求联网搜索又一城附近100港币内的高蛋白晚餐 | 返回 OpenRice、Eatigo、foodpanda 等真实搜索来源；证据不足时说明价格和匹配限制，没有伪造两份菜单 |
| 趋势 → 7天/14天 | 范围、体重及训练历史随选择更新 |
| 服务停止重启后刷新页面 | 独立 Cookie 无首屏会话冲突，服务使用保留的数据库 |
| 输入“我刚喝了一盒250毫升纯牛奶，帮我记为下午加餐。” | 最终版本执行真实记餐并返回简短保存回执、修改和撤销按钮；不再由模型重复计算回执数字 |
| 一般 → 睡眠充足 → 睡眠不足 | 共享准备度依次为65/86/38，睡眠405/468/312分钟；真实评估完成，前两档保持现有训练 |
| 低恢复 → 查看顺延安排 → 确认休息并顺延 → 刷新 | 今日休息，拉类9/12→9/14、腿部9/14→9/16、推类9/16→9/18；只有确认后才生效 |
| 分析中点击“停止回复”，再输入新的状态查询 | 停止后输入框和发送恢复可用，新查询成功返回 |
| 查询当前午餐、加餐、摄入与休息状态 | 午餐1050、牛奶160、总摄入1810 kcal；确认今天休息已生效、拉类已顺延至9/14，与真实保存操作一致 |
| 我的 → 其他设置 → 重置本次演示 | 仅本次测试会话恢复为初始正常场景，真实恢复评估重新完成 |

## 浏览器发现并推动的修复

- 独立数据库与其他端口共用 Cookie 名称，首屏报会话无效：已改为独立 Cookie，复测首屏正常。
- 睡眠场景只改变前端局部预览、调用不存在的接口：改为后端持久化模拟手表输入，共享真实 Agent 状态，训练仍需 Apply。
- 明确“午餐份量改成一半”被判只读：已补齐有边界的语法和唯一目标解析，原句复测通过。
- 模型超时后旧 pending 快照禁用重试：已修复错误/取消后的终态同步；浏览器验证停止后可以立即继续对话。没有再次人为制造外部模型超时。
- 低恢复只口头给候选、新增饮用记餐只口头称已保存：已修复事件/意图与必需工具调用，原流程复测通过。
- 真实写入后模型复述旧餐食数字：成功写入后改用真实回执，取消多余模型回合；最终新增和后续事实查询均通过。
- 纯问候返回自然文本却被结构化输出校验拒绝：统一使用自然文本回复再封装，中文问候、致谢和英文问候均在浏览器通过。

上述核心流程已通过。交付前已通过界面清除本次测试记录，恢复初始正常场景；真实恢复评估完成，浏览器停留在 Today，服务继续运行。

演示数据输入明确标为模拟手表，模型回复与工具执行是真实服务。三档输入不会自动改动已保存的营养目标，也不会无依据自动加重；训练调整仍须确认。外部模型和搜索需要联网，响应时间会随供应商变化。
